---
import '../styles/mercantile.css';
import SensoryNav from '../components/supermarket/SensoryNav.astro';
import MercantileFooter from '../components/supermarket/MercantileFooter.astro';
import { products } from '../lib/products';

const produce = products.filter((p) => p.category === 'produce');
const bakery = products.filter((p) => p.category === 'bakery');
const tonicBar = products.filter((p) => p.category === 'juice-bar' && ['vitality-juice', 'matcha-tonic', 'immunity-shot', 'charcoal-cleanse', 'aloe-water'].includes(p.id));
const cafe = products.filter((p) => p.category === 'juice-bar' && ['ethiopian-beans', 'ceremonial-matcha', 'cold-brew', 'oat-latte', 'earl-grey-tea'].includes(p.id));
const pantry = products.filter((p) => p.category === 'pantry');
const deli = products.filter((p) => p.category === 'deli');
const dairyAlt = products.filter((p) => p.category === 'dairy-alt');
const sweetTreats = products.filter((p) => p.category === 'sweet-treats');

const heroBanners = [
  { href: '/garden', image: 'https://images.unsplash.com/photo-1542838132-92c53300491e?auto=format&fit=crop&w=1200&q=80', alt: 'Fresh produce', subtitle: 'Fresh Daily', title: 'The Garden' },
  { href: '/bakery', image: 'https://images.unsplash.com/photo-1509440159596-0249088772ff?auto=format&fit=crop&w=1200&q=80', alt: 'Artisan bakery', subtitle: 'Baked Today', title: 'The Bakery' },
  { href: '/tonic-bar', image: 'https://images.unsplash.com/photo-1622597467836-f3285f2131b8?auto=format&fit=crop&w=1200&q=80', alt: 'Tonic Bar', subtitle: 'Cold-Pressed Daily', title: 'Tonic Bar' },
  { href: '/pantry', image: 'https://images.unsplash.com/photo-1556742049-0cfed4f6a45d?auto=format&fit=crop&w=1200&q=80', alt: 'Pantry staples', subtitle: 'Staples & Essentials', title: 'The Pantry' },
];
---

<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <meta name="view-transition" content="same-origin" />
  <title>The Mercantile — Curated Grocery</title>
  <meta name="description" content="Ingredients with provenance. A supermarket for people who read the label." />
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Public+Sans:wght@300;400;500;600;700;800;900&display=swap" rel="stylesheet" />
</head>
<body class="mercantile-page">
  <SensoryNav />

  <main class="main-content pt-[64px]">

    {/* ── HERO BANNER CAROUSEL ── */}
    <section class="px-4 lg:px-6 pt-4">
      <div class="carousel-wrap">
        <div class="carousel-track" id="carousel-hero">
          {heroBanners.map((b) => (
            <a href={b.href} class="hero-banner-card relative aspect-[2/1] overflow-hidden rounded-[12px] bg-surface group flex-shrink-0">
              <img
                src={b.image}
                alt={b.alt}
                class="w-full h-full object-cover transition-transform duration-600 group-hover:scale-105"
                style="transition-timing-function: cubic-bezier(0.4, 0, 0.2, 1);"
                loading="eager"
              />
              <div class="absolute inset-0 flex flex-col items-center justify-center text-center">
                <p class="text-[10px] font-bold tracking-[0.02em] uppercase text-white/70 mb-1">{b.subtitle}</p>
                <h2 class="text-2xl lg:text-4xl tracking-tight text-white">{b.title}</h2>
              </div>
            </a>
          ))}
        </div>
        <button class="carousel-arrow right" data-scroll-target="carousel-hero" aria-label="Scroll right">
          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M9 18l6-6-6-6"/></svg>
        </button>
      </div>
    </section>

    {/* ── PRODUCE GRID ── */}
    <section id="produce" class="px-4 lg:px-6 pt-10 scroll-reveal">
      <div class="flex items-baseline justify-between mb-4">
        <h3 class="text-[13px] font-bold tracking-[0.02em] uppercase text-obsidian-muted">Fresh Produce</h3>
        <a href="/garden" class="text-[12px] font-bold tracking-[0.02em] uppercase text-obsidian-muted hover:text-obsidian transition-colors">View All</a>
      </div>
      <div class="grid grid-cols-3 md:grid-cols-6 gap-4">
        {produce.slice(0, 6).map((product) => (
          <div class="product-card group" data-category={product.category}>
            <a href={`/supermarket/${product.id}`} class="block">
              <div class="card-image-wrap aspect-square w-full">
                <img src={product.image} alt={product.name} class="w-full h-full object-cover product-image" loading="lazy" style={`view-transition-name: product-${product.id}`} />
              </div>
            </a>
            <div class="pt-3">
              <p class="text-[10px] font-bold tracking-[0.02em] uppercase text-obsidian-muted">{product.label}</p>
              <h4 class="text-sm font-bold text-obsidian mt-0.5 leading-tight">{product.name}</h4>
              <div class="flex items-baseline justify-between mt-1">
                <span class="text-sm font-medium text-obsidian">${product.price.toFixed(2)}</span>
                <button data-add-product data-product-id={product.id} data-product-name={product.name} data-product-price={product.price} data-product-unit={product.size} data-product-image={product.image} class="btn-add">+ Add</button>
              </div>
            </div>
          </div>
        ))}
      </div>
    </section>

    {/* ── TONIC BAR GRID ── */}
    <section id="juice-bar" class="px-4 lg:px-6 pt-10 scroll-reveal">
      <div class="flex items-baseline justify-between mb-4">
        <h3 class="text-[13px] font-bold tracking-[0.02em] uppercase text-obsidian-muted">Tonic Bar</h3>
        <a href="/tonic-bar" class="text-[12px] font-bold tracking-[0.02em] uppercase text-obsidian-muted hover:text-obsidian transition-colors">View All</a>
      </div>
      <div class="grid grid-cols-3 md:grid-cols-6 gap-4">
        {tonicBar.slice(0, 6).map((product) => (
          <div class="product-card group" data-category={product.category}>
            <a href={`/supermarket/${product.id}`} class="block">
              <div class="card-image-wrap aspect-square w-full">
                <img src={product.image} alt={product.name} class="w-full h-full object-cover product-image" loading="lazy" style={`view-transition-name: product-${product.id}`} />
              </div>
            </a>
            <div class="pt-3">
              <p class="text-[10px] font-bold tracking-[0.02em] uppercase text-obsidian-muted">{product.label}</p>
              <h4 class="text-sm font-bold text-obsidian mt-0.5 leading-tight">{product.name}</h4>
              <div class="flex items-baseline justify-between mt-1">
                <span class="text-sm font-medium text-obsidian">${product.price.toFixed(2)}</span>
                <button data-add-product data-product-id={product.id} data-product-name={product.name} data-product-price={product.price} data-product-unit={product.size} data-product-image={product.image} class="btn-add">+ Add</button>
              </div>
            </div>
          </div>
        ))}
      </div>
    </section>

    {/* ── CAFE GRID ── */}
    <section id="cafe" class="px-4 lg:px-6 pt-10 scroll-reveal">
      <div class="flex items-baseline justify-between mb-4">
        <h3 class="text-[13px] font-bold tracking-[0.02em] uppercase text-obsidian-muted">Cafe</h3>
        <a href="/cafe" class="text-[12px] font-bold tracking-[0.02em] uppercase text-obsidian-muted hover:text-obsidian transition-colors">View All</a>
      </div>
      <div class="grid grid-cols-3 md:grid-cols-6 gap-4">
        {cafe.slice(0, 6).map((product) => (
          <div class="product-card group" data-category={product.category}>
            <a href={`/supermarket/${product.id}`} class="block">
              <div class="card-image-wrap aspect-square w-full">
                <img src={product.image} alt={product.name} class="w-full h-full object-cover product-image" loading="lazy" style={`view-transition-name: product-${product.id}`} />
              </div>
            </a>
            <div class="pt-3">
              <p class="text-[10px] font-bold tracking-[0.02em] uppercase text-obsidian-muted">{product.label}</p>
              <h4 class="text-sm font-bold text-obsidian mt-0.5 leading-tight">{product.name}</h4>
              <div class="flex items-baseline justify-between mt-1">
                <span class="text-sm font-medium text-obsidian">${product.price.toFixed(2)}</span>
                <button data-add-product data-product-id={product.id} data-product-name={product.name} data-product-price={product.price} data-product-unit={product.size} data-product-image={product.image} class="btn-add">+ Add</button>
              </div>
            </div>
          </div>
        ))}
      </div>
    </section>

    {/* ── BAKERY GRID ── */}
    <section id="bakery" class="px-4 lg:px-6 pt-10 scroll-reveal">
      <div class="flex items-baseline justify-between mb-4">
        <h3 class="text-[13px] font-bold tracking-[0.02em] uppercase text-obsidian-muted">The Bakery</h3>
        <a href="/bakery" class="text-[12px] font-bold tracking-[0.02em] uppercase text-obsidian-muted hover:text-obsidian transition-colors">View All</a>
      </div>
      <div class="grid grid-cols-3 md:grid-cols-6 gap-4">
        {bakery.slice(0, 6).map((product) => (
          <div class="product-card group" data-category={product.category}>
            <a href={`/supermarket/${product.id}`} class="block">
              <div class="card-image-wrap aspect-square w-full">
                <img src={product.image} alt={product.name} class="w-full h-full object-cover product-image" loading="lazy" style={`view-transition-name: product-${product.id}`} />
              </div>
            </a>
            <div class="pt-3">
              <p class="text-[10px] font-bold tracking-[0.02em] uppercase text-obsidian-muted">{product.label}</p>
              <h4 class="text-sm font-bold text-obsidian mt-0.5 leading-tight">{product.name}</h4>
              <div class="flex items-baseline justify-between mt-1">
                <span class="text-sm font-medium text-obsidian">${product.price.toFixed(2)}</span>
                <button data-add-product data-product-id={product.id} data-product-name={product.name} data-product-price={product.price} data-product-unit={product.size} data-product-image={product.image} class="btn-add">+ Add</button>
              </div>
            </div>
          </div>
        ))}
      </div>
    </section>

    {/* ── THE DELI GRID ── */}
    <section id="deli" class="px-4 lg:px-6 pt-10 scroll-reveal">
      <div class="flex items-baseline justify-between mb-4">
        <h3 class="text-[13px] font-bold tracking-[0.02em] uppercase text-obsidian-muted">The Deli</h3>
        <a href="/deli" class="text-[12px] font-bold tracking-[0.02em] uppercase text-obsidian-muted hover:text-obsidian transition-colors">View All</a>
      </div>
      <div class="grid grid-cols-3 md:grid-cols-6 gap-4">
        {deli.slice(0, 6).map((product) => (
          <div class="product-card group" data-category={product.category}>
            <a href={`/supermarket/${product.id}`} class="block">
              <div class="card-image-wrap aspect-square w-full">
                <img src={product.image} alt={product.name} class="w-full h-full object-cover product-image" loading="lazy" style={`view-transition-name: product-${product.id}`} />
              </div>
            </a>
            <div class="pt-3">
              <p class="text-[10px] font-bold tracking-[0.02em] uppercase text-obsidian-muted">{product.label}</p>
              <h4 class="text-sm font-bold text-obsidian mt-0.5 leading-tight">{product.name}</h4>
              <div class="flex items-baseline justify-between mt-1">
                <span class="text-sm font-medium text-obsidian">${product.price.toFixed(2)}</span>
                <button data-add-product data-product-id={product.id} data-product-name={product.name} data-product-price={product.price} data-product-unit={product.size} data-product-image={product.image} class="btn-add">+ Add</button>
              </div>
            </div>
          </div>
        ))}
      </div>
    </section>

    {/* ── PANTRY GRID ── */}
    <section id="pantry" class="px-4 lg:px-6 pt-10 scroll-reveal">
      <div class="flex items-baseline justify-between mb-4">
        <h3 class="text-[13px] font-bold tracking-[0.02em] uppercase text-obsidian-muted">The Pantry</h3>
        <a href="/pantry" class="text-[12px] font-bold tracking-[0.02em] uppercase text-obsidian-muted hover:text-obsidian transition-colors">View All</a>
      </div>
      <div class="grid grid-cols-3 md:grid-cols-6 gap-4">
        {pantry.slice(0, 6).map((product) => (
          <div class="product-card group" data-category={product.category}>
            <a href={`/supermarket/${product.id}`} class="block">
              <div class="card-image-wrap aspect-square w-full">
                <img src={product.image} alt={product.name} class="w-full h-full object-cover product-image" loading="lazy" style={`view-transition-name: product-${product.id}`} />
              </div>
            </a>
            <div class="pt-3">
              <p class="text-[10px] font-bold tracking-[0.02em] uppercase text-obsidian-muted">{product.label}</p>
              <h4 class="text-sm font-bold text-obsidian mt-0.5 leading-tight">{product.name}</h4>
              <div class="flex items-baseline justify-between mt-1">
                <span class="text-sm font-medium text-obsidian">${product.price.toFixed(2)}</span>
                <button data-add-product data-product-id={product.id} data-product-name={product.name} data-product-price={product.price} data-product-unit={product.size} data-product-image={product.image} class="btn-add">+ Add</button>
              </div>
            </div>
          </div>
        ))}
      </div>
    </section>

    {/* ── DAIRY ALT GRID ── */}
    <section id="dairy-alt" class="px-4 lg:px-6 pt-10 scroll-reveal">
      <div class="flex items-baseline justify-between mb-4">
        <h3 class="text-[13px] font-bold tracking-[0.02em] uppercase text-obsidian-muted">Dairy Alt</h3>
        <a href="/dairy-alt" class="text-[12px] font-bold tracking-[0.02em] uppercase text-obsidian-muted hover:text-obsidian transition-colors">View All</a>
      </div>
      <div class="grid grid-cols-3 md:grid-cols-6 gap-4">
        {dairyAlt.slice(0, 6).map((product) => (
          <div class="product-card group" data-category={product.category}>
            <a href={`/supermarket/${product.id}`} class="block">
              <div class="card-image-wrap aspect-square w-full">
                <img src={product.image} alt={product.name} class="w-full h-full object-cover product-image" loading="lazy" style={`view-transition-name: product-${product.id}`} />
              </div>
            </a>
            <div class="pt-3">
              <p class="text-[10px] font-bold tracking-[0.02em] uppercase text-obsidian-muted">{product.label}</p>
              <h4 class="text-sm font-bold text-obsidian mt-0.5 leading-tight">{product.name}</h4>
              <div class="flex items-baseline justify-between mt-1">
                <span class="text-sm font-medium text-obsidian">${product.price.toFixed(2)}</span>
                <button data-add-product data-product-id={product.id} data-product-name={product.name} data-product-price={product.price} data-product-unit={product.size} data-product-image={product.image} class="btn-add">+ Add</button>
              </div>
            </div>
          </div>
        ))}
      </div>
    </section>

    {/* ── SWEET TREATS GRID ── */}
    <section id="sweet-treats" class="px-4 lg:px-6 py-10 scroll-reveal">
      <div class="flex items-baseline justify-between mb-4">
        <h3 class="text-[13px] font-bold tracking-[0.02em] uppercase text-obsidian-muted">Sweet Treats</h3>
        <a href="/sweet-treats" class="text-[12px] font-bold tracking-[0.02em] uppercase text-obsidian-muted hover:text-obsidian transition-colors">View All</a>
      </div>
      <div class="grid grid-cols-3 md:grid-cols-6 gap-4">
        {sweetTreats.slice(0, 6).map((product) => (
          <div class="product-card group" data-category={product.category}>
            <a href={`/supermarket/${product.id}`} class="block">
              <div class="card-image-wrap aspect-square w-full">
                <img src={product.image} alt={product.name} class="w-full h-full object-cover product-image" loading="lazy" style={`view-transition-name: product-${product.id}`} />
              </div>
            </a>
            <div class="pt-3">
              <p class="text-[10px] font-bold tracking-[0.02em] uppercase text-obsidian-muted">{product.label}</p>
              <h4 class="text-sm font-bold text-obsidian mt-0.5 leading-tight">{product.name}</h4>
              <div class="flex items-baseline justify-between mt-1">
                <span class="text-sm font-medium text-obsidian">${product.price.toFixed(2)}</span>
                <button data-add-product data-product-id={product.id} data-product-name={product.name} data-product-price={product.price} data-product-unit={product.size} data-product-image={product.image} class="btn-add">+ Add</button>
              </div>
            </div>
          </div>
        ))}
      </div>
    </section>

  </main>

  <MercantileFooter />
</body>
</html>

<script>
  import { addToCart } from '../lib/cartStore';

  function showToast(message: string) {
    const toast = document.getElementById('toast');
    if (!toast) return;
    toast.textContent = message;
    toast.classList.add('visible');
    setTimeout(() => toast.classList.remove('visible'), 2500);
  }

  document.querySelectorAll('[data-add-product]').forEach((btn) => {
    btn.addEventListener('click', () => {
      const el = btn as HTMLElement;
      addToCart({
        id: el.dataset.productId!,
        name: el.dataset.productName!,
        price: parseFloat(el.dataset.productPrice!),
        unit: el.dataset.productUnit!,
        image: el.dataset.productImage!,
      });
      showToast('Added to bag');
    });
  });

  // Carousel arrow scrolling (hero only now)
  document.querySelectorAll('.carousel-arrow').forEach((arrow) => {
    arrow.addEventListener('click', () => {
      const targetId = (arrow as HTMLElement).dataset.scrollTarget!;
      const track = document.getElementById(targetId);
      if (track) track.scrollBy({ left: 460, behavior: 'smooth' });
    });
  });

  // Scroll-reveal: IntersectionObserver fade-up animation
  const revealSections = document.querySelectorAll('.scroll-reveal');
  const sectionObserver = new IntersectionObserver((entries) => {
    entries.forEach((entry) => {
      if (entry.isIntersecting) {
        entry.target.classList.add('revealed');
        sectionObserver.unobserve(entry.target);
      }
    });
  }, { threshold: 0.1 });

  revealSections.forEach((section) => sectionObserver.observe(section));

  // Product card fade-up on scroll
  const cards = document.querySelectorAll('.product-card');
  const cardObserver = new IntersectionObserver((entries) => {
    entries.forEach((entry) => {
      if (entry.isIntersecting) {
        entry.target.classList.add('visible');
        cardObserver.unobserve(entry.target);
      }
    });
  }, { threshold: 0.1 });

  cards.forEach((card, i) => {
    (card as HTMLElement).style.transitionDelay = `${i % 6 * 0.08}s`;
    cardObserver.observe(card);
  });
</script>
